<div class="row">
    <div class="col-6">


<%= form_with(model:task, local: true) do |f| %>
    <% if task.errors.any? %>
    <div id="error_explation class="alert alert-warning">
        <ul class="mb-0">
            <% task.errors.full_messages.each do |tsk| %>
            <li><%= tsk %></li>
            <% end %>
        </ul>
    </div>
    <% end %>
        
    <div class-"form-group">
      <%= f.label :status, 'ステータス' %>
      <%= f.text_field :status, class: 'form-control' %>
    </div>
    <div class-"form-group">  
      <%= f.label :content, 'タスク' %>
      <%= f.text_field :content, class: 'form-control' %>
    </div>
      
      <%= f.submit '作成', class: 'btn btn-primary' %>
<% end %>

        
    </div>
</div>

<%# ステータス欄に入れても、空欄エラーになった要因→text.fieldのとこにtext形式を入れていたから。 %>
<%# f.label:　と f.text_field :のとこは同じ文字（カラム名）を入れる必要がある %>
<%# というかstrong parametereのpermitで定めたハッシュ＝バリデートで定めたハッシュ＝フォームでの記載で統一させる %>